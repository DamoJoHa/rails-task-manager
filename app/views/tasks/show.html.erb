<h1><%= @task.title %></h1>
<p><%= @task.completed ? "Complete" : "Pending" %></p>
<%= link_to "Edit", edit_path(@task), class: "button"%>
<p><%= @task.details %></p>
<%= link_to "Back to list", tasks_path %>
